name: Web Deploy (prod)

# Continuous deployment for the self-hosted prod box.
#
# Trigger: CI ("CI" workflow) completes successfully on `main`.
# `workflow_run` runs in the repo's own context (not the PR's), so fork PRs
# cannot trigger this workflow even if their CI passes — but we still gate
# explicitly on branch + event below as a defence in depth.
#
# The self-hosted runner must:
#   - have the labels `self-hosted` AND `showbook-prod`
#   - have docker, docker compose, git, and pnpm on PATH
#   - have a checked-out prod tree at $PROD_DIR (default /opt/showbook)
#     containing a populated `.env.prod` file. The deploy fetches + resets
#     this tree to the deploy SHA, then runs `pnpm prod:up && pnpm prod:db:migrate`.
#
# See README.md → "Continuous deployment" for runner install steps.

on:
  workflow_run:
    workflows: ["CI"]
    types: [completed]
    branches: [main]
  workflow_dispatch:
    inputs:
      sha:
        description: "Commit SHA to deploy (defaults to latest main)"
        required: false
        default: ""

# Default to read-only — the deploy job runs on a self-hosted runner that
# operates on a checked-out tree via git/docker; it doesn't need to write to
# the GitHub repo via the GITHUB_TOKEN. `packages: read` lets the runner
# log in to GHCR and pull the prebuilt prod web image that CI pushed.
permissions:
  contents: read
  packages: read

concurrency:
  group: deploy-prod
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy to self-hosted prod
    # workflow_run: only deploy if CI passed AND was on main AND wasn't a fork PR.
    # workflow_dispatch: always allowed (manual trigger from the Actions tab).
    if: >-
      ${{ github.event_name == 'workflow_dispatch' ||
          (github.event.workflow_run.conclusion == 'success' &&
           github.event.workflow_run.head_branch == 'main' &&
           github.event.workflow_run.event != 'pull_request') }}
    runs-on: [self-hosted, showbook-prod]
    timeout-minutes: 30
    env:
      # Override via repo-level Variables (Settings → Variables → Actions)
      # if your prod tree lives somewhere other than /opt/showbook.
      PROD_DIR: ${{ vars.PROD_DIR || '/opt/showbook' }}
      DEPLOY_SHA: ${{ github.event.workflow_run.head_sha || inputs.sha }}
      # On macOS self-hosted runners, /usr/bin/git is a stub that delegates
      # to the developer dir selected by `xcode-select`. If the runner's
      # selected dir is full Xcode and the license hasn't been accepted,
      # every git invocation aborts with exit 69. Pinning DEVELOPER_DIR to
      # Command Line Tools avoids that prompt — its license is accepted at
      # install time and it has all we need (git, etc.). The variable is a
      # no-op on Linux runners and on Macs without CLT, where git falls
      # back to whatever's on PATH.
      DEVELOPER_DIR: /Library/Developer/CommandLineTools
    steps:
      - name: Verify prod tree
        # Check only operator-managed state pre-reset. The compose file
        # is tracked, so `git reset --hard` below produces it; checking
        # the path here would block the first deploy across the
        # docker-compose.prod.yml → infra/docker-compose.prod.yml move
        # because the old tree at PROD_DIR hasn't been reset yet.
        run: |
          set -euo pipefail
          test -d "$PROD_DIR/.git"      || { echo "PROD_DIR is not a git checkout: $PROD_DIR"; exit 1; }
          test -f "$PROD_DIR/.env.prod" || { echo ".env.prod missing in $PROD_DIR"; exit 1; }

      - name: Fetch latest main
        working-directory: ${{ env.PROD_DIR }}
        run: git fetch --prune origin main

      - name: Resolve deploy SHA
        working-directory: ${{ env.PROD_DIR }}
        run: |
          set -euo pipefail
          if [ -z "${DEPLOY_SHA}" ]; then
            DEPLOY_SHA=$(git rev-parse origin/main)
          fi
          echo "DEPLOY_SHA=$DEPLOY_SHA" >> "$GITHUB_ENV"
          echo "Deploying $DEPLOY_SHA to $PROD_DIR"

      - name: Reset prod tree to deploy SHA
        working-directory: ${{ env.PROD_DIR }}
        run: |
          set -euo pipefail
          git reset --hard "$DEPLOY_SHA"
          # Remove untracked tracked-adjacent files but keep gitignored state
          # (.env.prod, node_modules, .next caches, etc.).
          git clean -fd

      - name: Log in to GHCR
        # `pnpm prod:up` does `docker compose pull web` against
        # ghcr.io/ethanasm/showbook-web:latest; that image lives in a
        # private GHCR package by default, so the runner needs a docker
        # login before pull. The GITHUB_TOKEN is workflow-scoped and
        # auto-expires when the job finishes.
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin

      - name: Capture current image for rollback
        working-directory: ${{ env.PROD_DIR }}
        # Tag the currently-running web image as `:rollback` BEFORE prod:up
        # pulls the new `:latest` over it. Capturing the image *id* (not the
        # `:latest` tag, which is about to move) is what makes the rollback
        # below land on the previous build regardless of how it was tagged.
        run: |
          set -euo pipefail
          if docker inspect showbook-prod-web >/dev/null 2>&1; then
            CURRENT_IMAGE_ID=$(docker inspect --format '{{.Image}}' showbook-prod-web)
            docker tag "$CURRENT_IMAGE_ID" ghcr.io/ethanasm/showbook-web:rollback
            echo "HAVE_ROLLBACK=1" >> "$GITHUB_ENV"
            echo "Tagged $CURRENT_IMAGE_ID as :rollback (previous running image)"
          else
            echo "HAVE_ROLLBACK=0" >> "$GITHUB_ENV"
            echo "No running showbook-prod-web container — first deploy, no rollback target"
          fi

      - name: Pull + start prod containers
        working-directory: ${{ env.PROD_DIR }}
        run: pnpm prod:up

      - name: Pre-migration DB backup
        working-directory: ${{ env.PROD_DIR }}
        # Snapshot the prod DB before migrations run so a destructive
        # migration has a recovery point. Writes
        # $PROD_DIR/backups/pre-migrate-$DEPLOY_SHA.dump (distinct from the
        # nightly host-cron backup in scripts/backup-postgres.mjs). A backup
        # we can't take is exactly when we least want to migrate, so a
        # failure here is fatal and blocks the migration. A fresh/empty DB on
        # first deploy still dumps cleanly, so there's no first-deploy carve-out.
        run: pnpm prod:db:backup

      - name: Apply DB migrations
        working-directory: ${{ env.PROD_DIR }}
        run: pnpm prod:db:migrate

      - name: Post-deploy health gate
        working-directory: ${{ env.PROD_DIR }}
        # Block the deploy "done" until the new container reports ready
        # (DB reachable + pg-boss started). /api/health/ready returns 503
        # until both pass; the curl retries cover the boot/migration window.
        # On failure the rollback step below fires.
        run: pnpm prod:health:ready

      - name: Roll back web image on failure
        if: failure()
        working-directory: ${{ env.PROD_DIR }}
        # Fires when any step above failed — in practice the health gate.
        # Re-point the web container at the previous image (no pull: the
        # :rollback tag is local) and re-check. Forward migrations stay
        # applied; a DB restore is deliberately left manual (auto-restoring
        # would lose writes between the snapshot and the failure).
        run: |
          set -uo pipefail
          {
            echo "### ⚠️ Deploy failed — post-deploy health gate did not pass"
            echo ""
            echo "- SHA attempted: \`$DEPLOY_SHA\`"
          } >> "$GITHUB_STEP_SUMMARY"
          if [ "${HAVE_ROLLBACK:-0}" = "1" ]; then
            echo "Rolling web back to the previously-running image (:rollback)…"
            WEB_IMAGE_TAG=rollback docker compose --env-file .env.prod -f infra/docker-compose.prod.yml up -d web
            if pnpm prod:health:ready; then
              echo "- Rollback: ✅ previous image healthy again" >> "$GITHUB_STEP_SUMMARY"
            else
              echo "- Rollback: ❌ previous image ALSO unhealthy — manual intervention required" >> "$GITHUB_STEP_SUMMARY"
            fi
          else
            echo "- Rollback: skipped (no previous image — first deploy)" >> "$GITHUB_STEP_SUMMARY"
          fi
          {
            echo ""
            echo "**Forward DB migrations remain applied.** If the migration was the cause, restore the pre-migration snapshot manually on the prod host:"
            echo '```bash'
            echo "docker compose --env-file .env.prod -f infra/docker-compose.prod.yml cp \"backups/pre-migrate-$DEPLOY_SHA.dump\" db:/tmp/restore.dump"
            echo "docker compose --env-file .env.prod -f infra/docker-compose.prod.yml exec -T db \\"
            echo "  pg_restore --clean --if-exists --no-owner --no-acl -U showbook_prod -d showbook_prod /tmp/restore.dump"
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
          exit 1

      # The mobile-e2e backend (infra/docker-compose.e2e.yml) runs the
      # same :latest image as prod. Refresh it here — while this job's
      # GHCR login is still valid and right after prod:up updated the
      # local :latest tag — so the e2e backend's API never drifts from
      # prod's. The skip is silent ONLY on a host that has never set the
      # stack up (no .env.e2e AND no e2e containers); if the stack exists
      # but .env.e2e is gone, this fails the job — prod has already
      # rolled by this point, so failing only surfaces the config rot.
      # (A silent skip here once left the e2e web container running a
      # 3-week-stale image while every deploy quietly passed it by.)
      # A refresh that runs but fails stays a warning: a broken e2e
      # backend should fail the mobile-e2e workflow, not prod deploys.
      - name: Refresh e2e backend (if configured)
        working-directory: ${{ env.PROD_DIR }}
        run: |
          set -uo pipefail
          if [ ! -f .env.e2e ]; then
            if docker ps -a --format '{{.Names}}' | grep -q 'showbook-e2e-'; then
              echo "::error::.env.e2e is missing from $PROD_DIR but the showbook-e2e stack exists — every deploy is silently skipping the e2e refresh. Recreate .env.e2e (POSTGRES_PASSWORD + AUTH_SECRET; see the infra/docker-compose.e2e.yml header) or tear the stack down."
              exit 1
            fi
            echo "No .env.e2e in $PROD_DIR and no e2e stack — skipping e2e backend refresh"
            exit 0
          fi
          if ! (pnpm e2e:up && pnpm e2e:db:migrate); then
            echo "::warning::e2e backend refresh failed — mobile e2e may run against a stale backend"
            exit 0
          fi
          # Wait out the healthcheck start_period plus a few probes so a
          # container that recreates cleanly but then crash-loops (bad
          # env, failed boot validation) is caught here, not by the next
          # Maestro run.
          status=unknown
          for _ in $(seq 1 24); do
            status="$(docker inspect --format '{{.State.Health.Status}}' showbook-e2e-web 2>/dev/null || echo unknown)"
            if [ "$status" = "healthy" ]; then
              echo "e2e backend refreshed and healthy"
              exit 0
            fi
            sleep 5
          done
          echo "::warning::showbook-e2e-web is not healthy after refresh (status: $status) — mobile e2e will fail until it recovers"

      - name: Prune old images and build cache
        run: |
          set -euo pipefail
          docker image prune -f
          docker image prune -af --filter "until=720h"
          docker buildx prune -af --filter "until=720h"

      - name: Summary
        if: always()
        run: |
          {
            echo "### Deploy result"
            echo ""
            echo "- SHA: \`$DEPLOY_SHA\`"
            echo "- Target: \`$PROD_DIR\`"
            echo "- Status: ${{ job.status }}"
          } >> "$GITHUB_STEP_SUMMARY"
