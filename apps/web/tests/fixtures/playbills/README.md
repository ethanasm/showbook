# Playbill fixtures

`Hadestown.png` is the fixture used by `tests/playbill-cast-extract.spec.ts`
to verify that Groq's vision model extracts only the principal cast from a
real playbill — no swings, understudies, or orchestra.

If you add more playbills, name them descriptively and reference them from
new specs.
